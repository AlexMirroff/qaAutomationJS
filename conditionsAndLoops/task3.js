const NUMBER = 7

for (let i = 1; i <= 10; i++) {

    let output = `${NUMBER} x ${i} = ${NUMBER * i}`
    console.log(output)
}

let i = 1
while (i <= 10) {

    let output = `${NUMBER} x ${i} = ${NUMBER * i}`
    console.log(output)
    i++
}
